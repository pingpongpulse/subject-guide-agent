# 🔄 Frontend Migration Guide: Formula Sheet Integration\n\n## Quick Update for Streamlit UI\n\nThis guide shows how to update your frontend to display the optional formula field alongside summaries.\n\n---\n\n## Before & After\n\n### ❌ Old Code (Before)\n\n```python\nfrom agents.summary_agent import generate_summary\n\n# Generate summary\nresult = generate_summary(query, mode=\"standard\")\n\n# Display\nst.markdown(result[\"content\"])\n\nif result.get(\"sources\"):\n    st.markdown(\"## Sources\")\n    for source in result[\"sources\"]:\n        st.write(source)\n```\n\n### ✅ Updated Code (After)\n\n```python\nfrom agents.summary_agent import generate_summary\n\n# Generate summary (now includes optional formulas)\nresult = generate_summary(query, mode=\"standard\")\n\n# Display summary\nst.markdown(result[\"content\"])\n\n# Display formulas if present (NEW)\nif result.get(\"formulas\"):\n    st.markdown(\"## 📐 Key Formulas\")\n    for i, formula in enumerate(result[\"formulas\"], 1):\n        with st.expander(f\"Formula {i}: {formula['formula'][:50]}...\"):\n            st.latex(formula[\"formula\"])\n            st.write(f\"**Variables:** {formula['variables']}\")\n            st.write(f\"**Explanation:** {formula['explanation']}\")\n            st.write(f\"**Use Case:** {formula['use_case']}\")\n\n# Display sources\nif result.get(\"sources\"):\n    st.markdown(\"## Sources\")\n    for source in result[\"sources\"]:\n        st.write(source)\n```\n\n---\n\n## Migration Steps\n\n### Step 1: Update Summary Display Component\n\nFind your summary rendering code and add formula section:\n\n```python\ndef display_summary(summary_data):\n    \"\"\"Display summary with optional formula section\"\"\"\n    \n    # Summary content (existing)\n    st.markdown(summary_data[\"content\"])\n    \n    # ADD THIS: Formula section\n    if summary_data.get(\"formulas\"):\n        with st.expander(\"📐 Formulas & Equations\"):\n            for formula_obj in summary_data[\"formulas\"]:\n                st.latex(formula_obj[\"formula\"])\n                st.caption(f\"Variables: {formula_obj['variables']}\")  \n                st.caption(f\"Use: {formula_obj['use_case']}\")\n    \n    # Sources (existing)\n    if summary_data.get(\"sources\"):\n        st.markdown(\"## Sources\")\n        for source in summary_data[\"sources\"]:\n            st.write(source)\n```\n\n### Step 2: Side-by-Side Rendering (Optional)\n\nFor better UX, display summary and formulas side-by-side:\n\n```python\ndef display_summary_with_formulas(summary_data):\n    \"\"\"Display summary and formulas in two columns\"\"\"\n    \n    col1, col2 = st.columns(2, gap=\"large\")\n    \n    # Left column: Summary\n    with col1:\n        st.markdown(\"### Summary\")\n        st.markdown(summary_data[\"content\"])\n        \n        if summary_data.get(\"sources\"):\n            st.markdown(\"#### Sources\")\n            for source in summary_data[\"sources\"]:\n                st.caption(source)\n    \n    # Right column: Formulas (if present)\n    with col2:\n        if summary_data.get(\"formulas\"):\n            st.markdown(\"### 📐 Formulas\")\n            for i, formula in enumerate(summary_data[\"formulas\"], 1):\n                st.latex(formula[\"formula\"])\n                st.caption(f\"**Variables:**\\n{formula['variables']}\")\n                st.caption(f\"**Explanation:**\\n{formula['explanation']}\")\n                st.caption(f\"**Use Case:**\\n{formula['use_case']}\")\n                st.divider()\n        else:\n            st.info(\"No formulas found in content\")\n```\n\n### Step 3: Error Handling\n\nEnsure your code gracefully handles both cases:\n\n```python\ndef safe_display_summary(summary_data):\n    \"\"\"Safely display summary with formula fallback\"\"\"\n    \n    try:\n        # Display main content\n        st.markdown(summary_data.get(\"content\", \"No content\"))\n        \n        # Check if formulas exist (optional field)\n        formulas = summary_data.get(\"formulas\")\n        \n        if formulas:\n            st.markdown(\"## 📐 Formulas & Equations\")\n            try:\n                for i, formula in enumerate(formulas, 1):\n                    try:\n                        st.latex(formula.get(\"formula\", \"N/A\"))\n                        st.write(f\"Variables: {formula.get('variables', 'N/A')}\")\n                    except Exception as e:\n                        st.warning(f\"Formula {i} display error: {e}\")\n            except Exception as e:\n                st.warning(f\"Formula section error: {e}\")\n        \n        # Display sources\n        sources = summary_data.get(\"sources\", [])\n        if sources:\n            st.markdown(\"## Sources\")\n            for source in sources:\n                st.write(f\"- {source}\")\n                \n    except Exception as e:\n        st.error(f\"Display error: {e}\")\n```\n\n---\n\n## Component Library Examples\n\n### Component 1: Formula Card\n\n```python\ndef render_formula_card(formula_obj, index=1):\n    \"\"\"Render a single formula as a card\"\"\"\n    \n    with st.container(border=True):\n        st.markdown(f\"### Formula {index}\")\n        st.latex(formula_obj[\"formula\"])\n        \n        cols = st.columns(2)\n        with cols[0]:\n            st.markdown(\"**Variables**\")\n            st.caption(formula_obj[\"variables\"])\n        \n        with cols[1]:\n            st.markdown(\"**Use Case**\")\n            st.caption(formula_obj[\"use_case\"])\n        \n        st.markdown(\"**Explanation**\")\n        st.write(formula_obj[\"explanation\"])\n```\n\n### Component 2: Formula Sheet (Printable)\n\n```python\ndef render_printable_formula_sheet(summary_data):\n    \"\"\"Create a printable formula sheet\"\"\"\n    \n    if not summary_data.get(\"formulas\"):\n        st.info(\"No formulas to print\")\n        return\n    \n    # Create printable content\n    html_content = f\"\"\"<h1>{summary_data['title']}</h1>\"\"\"\n    \n    for formula in summary_data[\"formulas\"]:\n        html_content += f\"\"\"\n        <div style=\"page-break-inside: avoid; margin-bottom: 20px;\">\n            <h3>{formula['formula']}</h3>\n            <p><strong>Variables:</strong> {formula['variables']}</p>\n            <p><strong>Use:</strong> {formula['use_case']}</p>\n        </div>\n        \"\"\"\n    \n    st.markdown(html_content, unsafe_allow_html=True)\n    \n    # Print button\n    if st.button(\"🖨️ Print Formula Sheet\"):\n        st.info(\"Use browser print (Ctrl+P / Cmd+P)\")\n```\n\n### Component 3: Formula Search/Filter\n\n```python\ndef render_formula_filter(summary_data):\n    \"\"\"Allow filtering formulas by search term\"\"\"\n    \n    if not summary_data.get(\"formulas\"):\n        return\n    \n    search_term = st.text_input(\"🔍 Search formulas...\").lower()\n    \n    filtered = [\n        f for f in summary_data[\"formulas\"]\n        if search_term in f.get(\"formula\", \"\").lower()\n        or search_term in f.get(\"explanation\", \"\").lower()\n    ]\n    \n    st.write(f\"Found {len(filtered)} formula(s)\")\n    \n    for i, formula in enumerate(filtered, 1):\n        render_formula_card(formula, i)\n```\n\n---\n\n## Common Update Patterns\n\n### Pattern 1: Conditional Tabs\n\n```python\ntab1, tab2 = st.tabs([\"📝 Summary\", \"📐 Formulas\"])\n\nwith tab1:\n    st.markdown(summary_data[\"content\"])\n\nwith tab2:\n    if summary_data.get(\"formulas\"):\n        for formula in summary_data[\"formulas\"]:\n            st.latex(formula[\"formula\"])\n    else:\n        st.info(\"No formulas in this summary\")\n```\n\n### Pattern 2: Expandable Sections\n\n```python\nif summary_data.get(\"formulas\"):\n    with st.expander(f\"📐 {len(summary_data['formulas'])} Formulas\", expanded=True):\n        for i, formula in enumerate(summary_data[\"formulas\"], 1):\n            with st.expander(f\"Formula {i}\", expanded=False):\n                st.latex(formula[\"formula\"])\n                st.caption(formula[\"explanation\"])\n```\n\n### Pattern 3: Scrollable Container\n\n```python\nif summary_data.get(\"formulas\"):\n    st.markdown(\"### 📐 Available Formulas\")\n    \n    # Horizontal scroll container\n    formula_cols = st.columns(len(summary_data[\"formulas\"]))\n    \n    for i, (col, formula) in enumerate(zip(formula_cols, summary_data[\"formulas\"])):\n        with col:\n            st.markdown(f\"**Formula {i+1}**\")\n            st.latex(formula[\"formula\"])\n            st.caption(formula[\"use_case\"])\n```\n\n---\n\n## Testing Your Updates\n\n### Test 1: With Formulas\n\n```python\n# Test summary that should have formulas\nresult = generate_summary(\n    \"Explain Newton's Laws\",\n    mode=\"detailed\"\n)\n\nassert \"formulas\" in result, \"Formulas not in output\"\nassert len(result[\"formulas\"]) > 0, \"No formulas extracted\"\nassert all(k in f for f in result[\"formulas\"] \n           for k in [\"formula\", \"variables\", \"explanation\", \"use_case\"]), \\\n    \"Missing keys in formula objects\"\n```\n\n### Test 2: Without Formulas\n\n```python\n# Test summary likely without formulas\nresult = generate_summary(\n    \"Explain literary analysis\",\n    mode=\"standard\"\n)\n\n# Should work fine whether formulas exist or not\nif result.get(\"formulas\"):\n    print(f\"Found {len(result['formulas'])} formulas\")\nelse:\n    print(\"No formulas in this summary\")\n```\n\n---\n\n## Backwards Compatibility\n\n### ✅ Your Old Code Will Still Work\n\n```python\n# This still works - formulas field is optional\nresult = generate_summary(query)\nst.markdown(result[\"content\"])\nst.markdown(f\"Sources: {', '.join(result['sources'])}\")\n```\n\n### ⚠️ But Check for the Field\n\n```python\n# Do this:\nif \"formulas\" in result:  # ✓ Safe\n    ...\n\n# Don't do this (will fail if field missing):\nfor f in result[\"formulas\"]:  # ✗ May error\n    ...\n\n# Do this instead:\nfor f in result.get(\"formulas\", []):  # ✓ Safe\n    ...\n```\n\n---\n\n## Performance Considerations\n\n### Rendering Optimization\n\n```python\n# ❌ Slow: Re-renders formulas every time\nif summary_data.get(\"formulas\"):\n    for formula in summary_data[\"formulas\"]:\n        st.latex(formula[\"formula\"])  # Re-renders all on each interaction\n\n# ✅ Fast: Cached rendering\n@st.cache_data\ndef render_formulas(formulas):\n    return [f[\"formula\"] for f in formulas]\n\nif summary_data.get(\"formulas\"):\n    for formula_latex in render_formulas(tuple(summary_data[\"formulas\"])):\n        st.latex(formula_latex)\n```\n\n---\n\n## Troubleshooting\n\n### Issue: Formulas Not Showing\n\n```python\n# Debug: Check what's in result\nif \"formulas\" not in result:\n    st.info(\"No formulas field in result\")\nelse:\n    st.info(f\"Found {len(result['formulas'])} formulas\")\n\n# Show raw data\nst.json(result)\n```\n\n### Issue: LaTeX Not Rendering\n\n```python\n# Make sure formula is a string\nif isinstance(formula[\"formula\"], str):\n    st.latex(formula[\"formula\"])\nelse:\n    st.error(f\"Formula should be string, got {type(formula['formula'])}\")\n```\n\n### Issue: Missing Fields in Formula\n\n```python\n# Validate formula structure\nrequired_keys = {\"formula\", \"variables\", \"explanation\", \"use_case\"}\nfor f in result.get(\"formulas\", []):\n    missing = required_keys - set(f.keys())\n    if missing:\n        st.warning(f\"Formula missing: {missing}\")\n```\n\n---\n\n## Summary: What Changed?\n\n| Aspect | Before | After |\n|--------|--------|-------|\n| Formula field | ❌ Not present | ✅ Optional field |\n| Output structure | `{title, content, sources, mode}` | `{title, content, sources, mode, [formulas]}` |\n| Breaking changes | N/A | ❌ None (fully backwards compatible) |\n| Code required | Existing code works | Add 5-10 lines for formulas display |\n| Formula availability | N/A | Extracted from retrieved content |\n\n---\n\n## Deployment Checklist\n\n- [ ] Updated summary display component\n- [ ] Added formula handling (with .get() safety)\n- [ ] Tested with formulas present\n- [ ] Tested with no formulas\n- [ ] LaTeX rendering working\n- [ ] Side-by-side layout looks good\n- [ ] Error handling in place\n- [ ] Performance acceptable\n- [ ] User feedback positive\n- [ ] Deployed to production\n\n---\n\n## Next Steps\n\n1. **Update your frontend code** using patterns above\n2. **Test thoroughly** with both scenarios (with/without formulas)\n3. **Review with formula sheet agent** team for any adjustments\n4. **Deploy and monitor** for any issues\n5. **Gather feedback** from users\n\n---\n\n**Status**: Ready for Implementation\n**Effort**: 30-60 minutes\n**Complexity**: Low (mostly copy-paste with your UI framework)\n